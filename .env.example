ARK_API_KEY=your_api_key_here
# Default to AP Southeast
API_BASE=https://ark.ap-southeast.bytepluses.com
# Optional: change port if needed
PORT=3000

# Optional protection (recommended for public hosting)
APP_USERNAME=me
APP_PASSWORD=change_this
# Optional: comma-separated IP allowlist (e.g., from Cloudflare)
# When set, only these IPs are allowed (before auth)
ALLOW_IPS=

